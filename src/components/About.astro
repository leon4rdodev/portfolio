---
import type { Lang } from "../i18n/ui";
import { useTranslations } from "../i18n/utils";
import userAvatar from "../assets/icon/leon4rdo.webp";

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);

const metaItems = [
  {
    label: t("about.meta.location.label"),
    value: t("about.meta.location.value"),
    icon: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>`,
  },
  {
    label: t("about.meta.timezone.label"),
    value: t("about.meta.timezone.value"),
    icon: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>`,
  },
  {
    label: t("about.meta.languages.label"),
    value: t("about.meta.languages.value"),
    icon: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>`,
  },
  {
    label: t("about.meta.availability.label"),
    value: t("about.meta.availability.value"),
    icon: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>`,
  },
  {
    label: t("about.meta.github.label"),
    value: t("about.meta.github.value"),
    href: "https://github.com/leon4rdodev",
    icon: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>`,
  },
];
---

<section class="about-outer" id="about">
  <div class="section-decor">
    <div class="glass-code a-2 animate-on-scroll">
      &#123; <span class="vn">"status"</span>: <span class="st">"available"</span> &#125;
    </div>
    <div class="glass-code a-4 animate-on-scroll">
      <span class="vn">bio</span>.<span class="fn">currentRole</span> = <span class="st">'Fullstack'</span>
    </div>
    <div class="glass-code a-5 animate-on-scroll">
      <span class="kw">git commit</span> -m <span class="st">"update bio"</span>
    </div>
    <div class="glass-code a-6 animate-on-scroll">
      <span class="vn">skills</span>.<span class="fn">push</span>(<span class="st">'Architecture'</span>)
    </div>
    <div class="glass-code a-7 animate-on-scroll">
      <span class="kw">const</span> <span class="vn">isRemote</span> = <span class="kw">true</span>
    </div>
    <div class="glass-code a-side-user animate-on-scroll">
      <span class="kw">git config</span> user.name <span class="st">"Leon4rdo"</span>
    </div>
  </div>
  <div class="about-inner container">

    <!-- Left: text -->
    <div class="about-text animate-on-scroll">
      <h2 class="about-heading">{t("about.title")}</h2>
      <div class="about-body">
        <p set:html={t("about.p1")} />
        <p set:html={t("about.p2")} />
        <p set:html={t("about.p3")} />
        <p set:html={t("about.p4")} />
      </div>
      <a href="#contact" class="btn-primary" style="margin-top:2rem">
        {t("about.cta")}
      </a>
    </div>

    <!-- Right: meta card -->
    <div class="about-card animate-on-scroll">
      <div class="about-badge">
        <div class="badge-avatar">
          <img
            src={userAvatar.src}
            alt="Leonardo Victoriano — Fullstack Developer"
            width="100"
            height="100"
            loading="lazy"
            decoding="async"
          />
        </div>
        <p class="badge-name">Leonardo Victoriano</p>
        <p class="badge-role">{t("hero.role")}</p>
      </div>

      <ul class="meta-list">
        {
          metaItems.map((item) => (
            <li class="mi">
              <span class="mi-icon" set:html={item.icon} />
              <div>
                <p class="mi-label">{item.label}</p>
                <p class="mi-val">
                  {item.href ? (
                    <a href={item.href} target="_blank" rel="noopener noreferrer">
                      {item.value}
                    </a>
                  ) : (
                    item.value
                  )}
                </p>
              </div>
            </li>
          ))
        }
      </ul>
    </div>

  </div>
</section>

<style>
  .about-outer {
    background: var(--bg);
    padding: 7rem 2rem 14rem;
    position: relative;
    border-bottom: 1px solid var(--border);
    overflow: hidden;
  }

  .about-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: start;
    position: relative;
    z-index: 1;
  }

  /* ── LEFT TEXT ── */
  .about-text .sec-label {
    color: var(--accent);
  }

  .about-heading {
    font-family: var(--display);
    font-size: clamp(2.8rem, 5vw, 4rem);
    line-height: 0.95;
    letter-spacing: 0.04em;
    color: var(--fg);
    margin-bottom: 2rem;
  }

  .about-body p {
    font-size: 0.95rem;
    line-height: 1.85;
    color: var(--fg-2);
    margin-bottom: 1.25rem;
  }

  .about-body p:last-child { margin-bottom: 0; }

  .about-body :global(em) {
    color: var(--fg);
    font-style: normal;
    font-weight: 600;
  }

  /* ── RIGHT CARD ── */
  .about-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-xl);
    box-shadow: 0 12px 40px rgba(0,0,0,0.03);
    overflow: hidden;
  }

  .about-badge {
    background: var(--surface);
    border-bottom: 1px solid var(--border);
    padding: 2rem;
    text-align: center;
  }

  .badge-avatar {
    width: 100px; height: 100px;
    border: 3px solid var(--border);
    border-radius: 50%;
    margin: 0 auto 1rem;
    overflow: hidden;
    background: var(--blue-50);
    box-shadow: 0 8px 24px rgba(37,99,235,0.08);
  }

  .badge-avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
    display: block;
  }

  .badge-name {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--fg);
    margin-bottom: 0.25rem;
  }

  .badge-role {
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--fg-3);
  }

  /* Meta list */
  .meta-list {
    padding: 0;
    list-style: none;
    margin: 0;
  }

  .mi {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem 1.75rem;
    border-bottom: 1px solid var(--border);
    transition: background 0.2s;
  }

  .mi:last-child { border-bottom: none; }
  .mi:hover { background: var(--blue-50); }

  .mi-icon {
    width: 36px; height: 36px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--accent);
    flex-shrink: 0;
  }

  .mi:hover .mi-icon {
    background: #fff;
    border-color: var(--blue-200);
  }

  .mi-icon :global(svg) { width: 16px; height: 16px; }

  .mi-label {
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--fg-3);
    margin-bottom: 0.18rem;
  }

  .mi-val {
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--fg-2);
  }

  .mi-val a {
    color: var(--accent);
    text-decoration: none;
    transition: color 0.2s;
  }

  .mi-val a:hover { color: var(--blue-700); text-decoration: underline; }

  /* ── DECOR ── */
  .a-1 { top: calc(7% - 20px); right: 6%; animation-delay: 0.5s; }
  .a-2 { bottom: 12%; left: 4%; animation-delay: 1.5s; }
  .a-4 { top: 92%; right: 15%; animation-delay: 2.5s; opacity: 0.5; }
  .a-5 { bottom: calc(6% + 200px); left: 50%; animation-delay: 0.7s; transform: translateX(-50%); }
  .a-6 { top: 4%; left: 22%; animation-delay: 1.8s; opacity: 0.5; }
  .a-7 { bottom: 20%; right: 3%; animation-delay: 3s; }
  .a-8 { top: 94%; left: 10%; animation-delay: 2.2s; transform: rotate(5deg); }

  .a-side-user {
    top: 50%;
    left: -20px;
    transform: translateY(-50%) rotate(-90deg);
    padding: 0.6rem 1.2rem;
    font-size: 0.7rem;
    opacity: 0.6;
    letter-spacing: 0.1em;
  }

  @media (max-width: 1400px) {
    .a-side-user { left: -40px; }
  }

  @media (max-width: 1100px) {
    .a-3, .a-4, .a-6, .a-8, .a-side-user { display: none; }
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .about-inner { grid-template-columns: 1fr; gap: 3rem; }
    .about-card { max-width: 480px; margin: 0 auto; width: 100%; box-sizing: border-box; }
  }

  @media (max-width: 600px) {
    .about-outer { padding: 5rem 1.5rem 8rem; }
    .about-card {
      border-radius: var(--radius-lg); 
      border-left: 1px solid var(--border);
      border-right: 1px solid var(--border);
    }
    .about-badge { padding: 1.5rem 1rem; }
    .mi { padding: 1rem 1.25rem; gap: 0.85rem; }
    .mi-label { font-size: 0.55rem; }
    .mi-val { font-size: 0.8rem; word-break: break-word; }
  }
</style>
